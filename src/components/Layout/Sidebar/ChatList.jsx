import React from 'react';
import { List, ListItem, ListItemButton, ListItemIcon, ListItemText } from '@mui/material';

const ChatList = () => {
  // Since we're not using chat history, this can be empty or used for other purposes
  return (
    <List sx={{ px: 1 }}>
      {/* No chat history items as per requirement */}
    </List>
  );
};

export default ChatList;